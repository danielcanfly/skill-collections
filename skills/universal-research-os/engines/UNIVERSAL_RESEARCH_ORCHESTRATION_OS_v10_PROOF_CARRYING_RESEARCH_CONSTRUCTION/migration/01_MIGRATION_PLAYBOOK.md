# Migration Playbook

All v9 and earlier artifacts require approved legacy migration. Freeze exact wrapper/payload SHA, preserve mature content, rebuild source snapshots and clause proofs, issue v10 lineage, run v10 admission, Audit OS v6, promotion and wrapper validation. Never carry forward legacy MERGE_READY.
