# V7 Repair Compiler

Every repair contract must define class-wide closure. A finding cannot be closed by editing only named examples. The closure matrix must identify the entire affected population, its expected row count, the executable selector, and the command that proves all rows are closed. Every retained direct-support material clause must be independently re-reviewed after repair.
