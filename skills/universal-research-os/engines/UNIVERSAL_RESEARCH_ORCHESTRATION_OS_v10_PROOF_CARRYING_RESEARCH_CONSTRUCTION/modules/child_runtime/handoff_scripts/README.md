# Scripts

These are the same executable validators/finaliser copied into `OUTPUT_SKELETON/qa/`. The child Session must work inside its production-candidate directory and execute the `qa/` copies. Install dependency with `python -m pip install pyyaml`. Do not replace the benchmark with a stub or delete failed tests.
