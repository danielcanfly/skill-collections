# Dispatcher-Authored Auditor Tests

At least ten topic-specific queries must be written by the master dispatcher before child execution. Query text and SHA are immutable. A child may migrate expected IDs only through an explicit node disposition and migration log.
