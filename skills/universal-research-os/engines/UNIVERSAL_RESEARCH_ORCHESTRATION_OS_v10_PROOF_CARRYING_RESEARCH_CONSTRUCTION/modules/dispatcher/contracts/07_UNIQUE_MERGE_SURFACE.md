# Unique Merge Surface

Only `knowledge/` may contain authoritative production nodes. Root-level `concepts/`, `patterns/`, `source-notes/`, `registries/`, `checklists/`, `evaluation/`, `compatibility/` or equivalent mirrors are forbidden.
