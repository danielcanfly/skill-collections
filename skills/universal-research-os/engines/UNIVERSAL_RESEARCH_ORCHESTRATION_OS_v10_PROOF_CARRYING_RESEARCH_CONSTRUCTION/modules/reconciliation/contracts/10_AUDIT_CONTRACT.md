# Audit Contract v10

Builder and validator are separate identities. Audit runs on a fresh extraction of the exact SHA-bound ZIP, read-only against the candidate tree. The validator registry is authoritative and must include evidence coverage, source metadata completeness, operational depth, portable reproduction, production-surface hygiene and zero-open-finding gates. Production requires all dimensions 100 and open P0/P1/P2 equal zero.
