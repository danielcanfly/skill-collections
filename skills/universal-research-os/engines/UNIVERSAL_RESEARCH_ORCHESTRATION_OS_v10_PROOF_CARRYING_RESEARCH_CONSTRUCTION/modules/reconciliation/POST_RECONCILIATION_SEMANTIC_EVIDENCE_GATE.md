# Post-Reconciliation Semantic and Evidence Gate

After global ID/source/claim/case reconciliation, recompute semantic template metrics, edge entailment, source-note depth, evidence-family independence, retrieval leakage and release identity across all output surfaces. If structural identity is stable but these fail, route to `modules/global_semantic_evidence_seal/`; do not restart child research unless source facts or ontology are materially wrong.
