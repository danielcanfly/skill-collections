# Proof-Carrying Research Construction

This module enforces source-first compilation. Direct-support claims are generated artifacts. A stage may advance only when its receipt binds the candidate tree SHA and every affected row.
