# Agent Usage Router — Global Semantic & Evidence Seal

## Use this module when
- Global reconciliation is structurally complete.
- Canonical IDs and ownership are stable.
- Pages remain semantically templated, evidence bindings overclaim, source notes are shallow, retrieval benchmarks leak answer information, or release surfaces disagree.

## Do not use when
- Core ontology is missing or wrong.
- Material claims are factually false and require fresh research.
- Child candidates are not independently MERGE_READY.

## Sequence
R0 immutable baseline → R1 identity/structure → R2 semantic de-templating → R3 evidence boundary repair → R4 source-note depth → R5 frozen retrieval rebuild → R6 all-surface reconstruction → R7 independent Audit OS v6 Production Trust Chain Hardened → R8 repair handoff.
