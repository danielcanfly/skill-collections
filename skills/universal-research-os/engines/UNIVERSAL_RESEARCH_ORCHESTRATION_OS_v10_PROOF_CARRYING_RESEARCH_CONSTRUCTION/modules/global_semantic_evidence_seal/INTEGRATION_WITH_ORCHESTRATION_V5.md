# Integration with Orchestration OS v10

Use this module after structural reconciliation when canonical identity is stable but semantic template saturation, evidence overclaim, shallow source notes, retrieval leakage or release metadata drift remains.

The module inherits v10 source identity, passage, edge, lineage, portable-reproduction and production-surface contracts. Its scripts diagnose and repair; they do not override Audit OS v6 or production promotion gates.

No topic-specific reference implementation is shipped in the runtime package. Only anonymized regression lessons may remain under `profiles/reference_cases/`.
